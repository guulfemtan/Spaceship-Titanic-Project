# Spaceship-Titanic-Project
In this project, we aim to build a baseline model using these features to predict the target variable Transported. The approach includes simple data preprocessing, handling missing values, encoding categorical variables, and training a LightGBM classifier. This pipeline provides a fast and effective way to generate predictions for the test set.
